# InstaPilot AI — environment variables
# Copy to .env (Docker) or .env.local (local dev); set the same on your host for production.
# ✅ = required   – = optional
# No keys are bundled — every value below is supplied by YOU. Configure your brand
# (name, niche, handles) in the app under Settings → Brand after first login.

# ── Branding (white-label) ────────────────────────────────────────────────────
BRAND_NAME=                         # – default app/brand name fallback (e.g. "Tasty Bites AI")
NEXT_PUBLIC_APP_NAME=               # – public app name shown in the browser title/UI

# ── Core ──────────────────────────────────────────────────────────────────────
DATABASE_URL=                       # ✅ PostgreSQL connection string
APP_ACCESS_KEY=                     # ✅ access key gating the dashboard login
SESSION_SECRET=                     # ✅ HMAC secret for the auth session cookie (middleware)
NEXT_PUBLIC_APP_URL=                # – public app URL

# ── AI providers ──────────────────────────────────────────────────────────────
GROK_API_KEY=                       # ✅ Groq key (comments, DMs, Whisper, content fallback)
GROK_API_URL=                       # – default https://api.groq.com/openai/v1
AI_MODEL_MAIN=                      # – main Groq model (llama-3.3-70b-versatile)
AI_MODEL_FAST=                      # – fast Groq fallback (llama-3.1-8b-instant)
GEMINI_API_KEY=                     # ✅ Google Gemini key (content, vision, TTS, music mood)
GEMINI_MODEL=                       # – optional Gemini model override
CEREBRAS_API_KEY=                   # – Cerebras key (per-task AI Config chains; can also be set in Settings)

# ── AI voiceover / TTS (YouTube Shorts narration, opt-in) ──────────────────────
TTS_PROVIDER=                       # – which TTS to try first: "groq" (default) | "canopy"
GROQ_TTS_MODEL=                     # – Groq Orpheus model (default canopylabs/orpheus-v1-english)
GROQ_TTS_VOICE=                     # – default Groq voice (default autumn) — UI picks per-brand
CANOPY_TTS_URL=                     # – optional self-hosted Orpheus/Canopy TTS endpoint URL
CANOPY_TTS_KEY=                     # – optional bearer key for the Canopy TTS endpoint
CANOPY_TTS_VOICE=                   # – default Canopy voice (default tara)

# ── Instagram / Meta ──────────────────────────────────────────────────────────
INSTAGRAM_ACCESS_TOKEN=             # ✅ long-lived IG user token
INSTAGRAM_BUSINESS_ACCOUNT_ID=      # ✅ IG Business account ID
INSTAGRAM_USERNAME=                 # – account handle (self-reply suppression)
FACEBOOK_PAGE_ACCESS_TOKEN=         # ✅ Page token — required to send/read DMs
FACEBOOK_PAGE_ID=                   # ✅ linked Facebook Page ID
FACEBOOK_APP_ID=                    # ✅ Meta app ID
FACEBOOK_APP_SECRET=                # ✅ Meta app secret — also used to verify webhook HMAC
WEBHOOK_VERIFY_TOKEN=               # ✅ Instagram webhook verification token

# ── Media hosting ─────────────────────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=              # ✅ media hosting (images + audio)
CLOUDINARY_UPLOAD_PRESET=           # ✅ unsigned upload preset
CLOUDINARY_API_KEY=                 # – for deleting media after publish
CLOUDINARY_API_SECRET=              # – for deleting media after publish

# ── Email ─────────────────────────────────────────────────────────────────────
RESEND_API_KEY=                     # ✅ daily report + alert emails
RESEND_FROM=                        # – from address for Resend
NOTIFICATION_EMAIL=                 # ✅ report recipient
DM_AUTO_REPLY=                      # – fallback DM text when AI is unavailable

# ── YouTube (Shorts mirroring + auto-poster) ───────────────────────────────────
YOUTUBE_CLIENT_ID=                  # – OAuth client ID (only if YouTube enabled)
YOUTUBE_CLIENT_SECRET=              # – OAuth client secret
YOUTUBE_REFRESH_TOKEN=              # – channel refresh token (scopes: upload, readonly, force-ssl, yt-analytics)
YOUTUBE_CHANNEL_ID=                 # – channel ID (display only)

# ── Music for Shorts ──────────────────────────────────────────────────────────
JAMENDO_CLIENT_ID=                  # – free Jamendo API client id (vision-selected background music)
